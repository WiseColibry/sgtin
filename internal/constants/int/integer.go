package constants

const (
	//All Others
	ALLINT = byte(000)

	//Point of sale
	POSINT = byte(001)

	//Full Case for Transport
	FULLINT = byte(010)

	//Reserved
	RESERVEDINT = byte(011)

	//Inner Pack Trade Item Grouping for Handling
	IPTIGfHINT = byte(100)

	//Reserved
	ANOTHERRESERVEDINT = byte(101)
	//Unit Load
	UNITLOADINT = byte(110)

	//Unit inside Trade Item or component inside a product not intended for individual sale
	UITIORCNOTINDSALEINT = byte(111)
)
